<div class="login">
  <div class="card">
    <div class="card-body">
      <h1>Editing <%= @business.name %></h1>

      <%= render 'form', business: @business %>

      <%= link_to 'Back', [@location, @business], class: "btn btn-secondary" %>
    </div>
  </div>
</div>
