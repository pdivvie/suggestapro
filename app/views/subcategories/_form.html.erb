<%= form_for ([@location, @subcategory]) do |form| %>
  <% if subcategory.errors.any? %>
    <% @subcategory.errors.full_messages.each do |error| %>
      <%= alert_generator error %>
    <% end %>
  <% end %>

  <div class="form-group">
    <%= form.text_field :title, placeholder: "Title...", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :category %> <br>
    <%= form.select :category_id, Category.all.collect { |category| [ category.name, category.id ] }, include_blank: true, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit "Create Subcategory", class: "btn btn-primary" %>
  </div>
<% end %>