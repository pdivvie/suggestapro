<div class="login">
  <div class="card">
    <div class="card-body">
      <h1>Editing Category</h1>

      <%= render 'form', category: @category %>

      <%= link_to 'Back', location_categories_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>
