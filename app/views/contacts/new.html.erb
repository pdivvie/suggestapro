<div class="login">
  <div class="card">
    <div class="card-body">
      <h1>New Contact</h1>

      <%= render 'form', contact: @contact %>

      <%= link_to 'Back', location_contacts_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>